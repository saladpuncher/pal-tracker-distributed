applications:
- name: tracker-allocations
  path: ./applications/allocations-server/build/libs/allocations-server.jar
  routes:
  - route: allocations-pal-{UNIQUE_IDENTIFIER}.apps.chicken.pal.pivotal.io-skibby01
  memory: 1G
  instances: 1
  env:
    REGISTRATION_SERVER_ENDPOINT: https://{REGISTRATION_SERVER_NAME}.apps.chicken.pal.pivotal.io-skibby01
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
  services:
  - tracker-allocations-database
